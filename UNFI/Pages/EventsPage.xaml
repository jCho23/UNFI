<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:views="clr-namespace:UNFI.Views;assembly=UNFI"
    xmlns:xfg="clr-namespace:XFGloss;assembly=XFGloss"
    x:Class="UNFI.Pages.EventsPage">

    <xfg:ContentPageGloss.BackgroundGradient>
        <xfg:Gradient Rotation="180">
            <xfg:GradientStep StepColor="#ffd7b5" StepPercentage="0" />
            <xfg:GradientStep StepColor="#ffb38a" StepPercentage=".3" />
            <xfg:GradientStep StepColor="#ff9248" StepPercentage=".8" />
        </xfg:Gradient>
    </xfg:ContentPageGloss.BackgroundGradient>

    <ContentPage.ToolbarItems>
        <ToolbarItem
            x:Name="AddPost"
            Priority="0" 
            Icon="Add.png" 
            Order="Primary"
            Activated="Handle_Activated"/>
   </ContentPage.ToolbarItems>
    
    <ListView
        x:Name="EventsListView"
        ItemsSource="{Binding MyPostsListViewItemSource}">
         <ListView.RowHeight>
                <OnPlatform x:TypeArguments="x:Int32">
                    <On Platform="iOS, Android" Value="120" />
                </OnPlatform>
            </ListView.RowHeight>

        <ListView.ItemTemplate>
            <DataTemplate>
                <views:EventsViewCell/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
